import React from 'react';
import PropTypes from "prop-types";


interface Props{
  icon:React.ReactElement;
}

const MaterialIcons = (props:Props) =>{
  const {icon} = props;
  return (
    <div>
    </div>
  );
}

export default MaterialIcons;
